syntax = "proto2";

import 'Common.proto';

message JobTitle
{
    required  string job_id = 1;
    repeated string job_name=2;
    map<string,Duration> served=3;
    required string organzation_id=4;
    repeated string events_id=5;
    repeated string judges_id=6;
}